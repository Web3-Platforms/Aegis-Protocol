{
  "name": "aegis-protocol",
  "version": "1.0.0",
  "private": true,
  "description": "Aegis Protocol — AI-gated cross-chain yield vault on Polkadot",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "start": "cd frontend && npm run start",
    "dev": "cd frontend && npm run dev",
    "lint": "cd frontend && npm run lint"
  }
}
